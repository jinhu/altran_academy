{
  "visible_filenames": [
    "yahtzee.hpp",
    "yahtzee.cpp",
    "yahtzee.tests.cpp",
    "instructions",
    "makefile",
    "cyber-dojo.sh"
  ],
  "filename_extension" : ".cpp",
  "display_name": "7 Big Data workshop, 2 parallel processes",

  "progress_regexs": [
    "Tests run\\: (\\d)+,(\\s)+Failures\\: (\\d)+",
    "OK \\((\\d)+ test(s)?\\)"
  ],

  "red_amber_green" : [
    "lambda { |output|",
    "  return :red   if /^Tests run: (\\d*),(\\s)+Failures: (\\d*)/.match(output)",
    "  return :green if /^OK \\((\\d*) test/.match(output)",
    "  return :amber",
    "}"
  ],
  "image_name": "cyberdojofoundation/gpp_assert"
}
